.venv/
__pycache__/
*.py[cod]

# Reproducible local/GitHub Actions products; durable evidence lives under
# research/evidence and frozen experiment inputs live under experiments.
artifacts/
data/raw/
dist/
paper/*.aux
paper/*.fdb_latexmk
paper/*.fls
paper/*.log
paper/*.out
paper/*.pdf
paper/**/*.aux
paper/**/*.bbl
paper/**/*.blg
paper/**/*.fdb_latexmk
paper/**/*.fls
paper/**/*.log
paper/**/*.out
paper/**/*.pdf
paper/**/*Notes.bib

# Exception: the two finished letters are deliverables, not build products, and
# are tracked so the repository alone is enough to read and publish them.
!paper/short/prl_letter.pdf
!paper/short/main_short.pdf

# Agent worktrees and session scratch (never part of the scientific record)
.claude/

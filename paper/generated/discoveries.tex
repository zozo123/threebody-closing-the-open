% Generated from release_claim records in research/DISCOVERY_RELEASE.json.
\sloppy
\paragraph{One Continuation Family.}
Within the frozen Li--Li--Liao catalog and the declared v1 gauge, the evidence supports a single continuation-connected component: the 135,445-row mass-grid adjacency graph is connected (270,087 adjacency edges), and every one of the 26 adversarially selected links that could have separated it was crossed bidirectionally by continuation in the Li chart. Chart-independence was tested on one of those 26 -- the pathological invariant-near-duplicate bridge -- which was repeated in a generic translation-reduced strict-periodic chart outside the Li ansatz; the other 25 were not repeated in a second chart. Explicit absence of branch hysteresis is recorded for the 5 macroscopic bottlenecks. No surviving rank-loss, directionality, chart-dependence, or worst-MST obstruction to connectivity was found. The competing two-set reading is a reading of the scale-invariant (T,L) projection, which on this sheet is demonstrably folded and non-injective, so two visible branches there do not by themselves establish two dynamical families.

\emph{Method.} Full-catalog adjacency plus bidirectional Li-chart continuation of 5 macroscopic MST cuts, 1 pathological invariant-near-duplicate bridge (which was additionally crossed in a generic strict-periodic chart, so it counts once as a link and twice as a chart), and the 20 globally worst MST chart jumps (run 31877974928) with no surviving disconnection; plus a 647-point shooting-rank recensus with tight re-correction of the 28 lowest-rank samples.

\emph{Limitations.} Numerical continuation statement for the declared catalog domain, not a theorem about the full planar three-body moduli space. Continuation was certified on 26 distinct adversarially chosen links (5 macroscopic bottlenecks, 1 invariant-near-duplicate bridge, the 20 globally worst MST chart jumps), not on all 135,444 MST edges; connectivity across the remainder rests on 0.001-grid adjacency plus the absence of any surviving obstruction. The bridge is one link crossed in two charts and must not be counted as two links. Chart-independence is demonstrated on exactly one of the 26 links. Per research/V1\_CONNECTIVITY\_CERTIFICATE\_2026-08-15.md item 4, only the pathological invariant-near-duplicate bridge was repeated in a generic strict-periodic chart; the 5 macroscopic bottlenecks and the 20 worst MST edges are Li-chart results. 'Without branch hysteresis' is recorded only for the 5 bottlenecks (certificate item 2); it is not asserted for the other 21 links. A negative adversarial search cannot exclude a separation finer than the 0.001 catalog grid, or outside the declared domain m1 in [0.8,1.1], m2 in [0.7,1.2] at m3=1. 'One component' is the decomposition best supported by this evidence under this gauge. It is not a disproof of the 2023 analysis on its own (T,L) terms, and it is not a statement about families of the general three-body problem.

\paragraph{Principal Lower Plus One.}
On the slice m1=0.8023446113666945 with m3=1, the lower stability change of the sheet is bracketed at m2 in [0.7540187211922449, 0.7540187227698909] (width 1.58e-9) by a sign change of the event function, and is independently reproduced in a separate Julia BigFloat stack, with canonical Jacobi monodromy and physical E\^{}omega/E diagnostics, as a physical +1 crossing of the reduced return map. Across that bracket the smaller-m2 flank carries a real reciprocal pair off the unit circle and the larger-m2 flank is elliptic.

\emph{Method.} Independent Julia BigFloat Vern9 correction and event bisection to a sign-change bracket, then canonical Jacobi monodromy and physical E\^{}omega/E evaluation on both flanks (run 31886247498).

\emph{Limitations.} Planar linear Floquet only. One bracketed representative on one m1 slice; it does not certify the mechanism along the rest of the +1 edge. Flank spectra are evaluated at a fixed +/-5e-6 offset in m2, not as a limit at the critical point. A sign-change bracket at 60 digits localizes a root of the computed event function; it is not an interval-arithmetic existence proof and does not separately certify transversality.

\paragraph{Principal Upper Hamiltonian Hopf.}
On the slice m1=0.8022889780964406 with m3=1, the upper stability change of the sheet is bracketed at m2 in [0.7592347034247101, 0.7592347096924873] (width 6.27e-9) by a sign change of the event function, and is independently reproduced in a separate Julia BigFloat stack as an opposite-Krein Hamiltonian-Hopf collision. The stable flank has two upper-half unit modes of opposite Krein sign; the unstable flank opens a reciprocal complex quartet (radius deviation 1.83e-2).

\emph{Method.} Independent Julia BigFloat event bracket plus physical E\^{}omega/E Krein diagnostics on both flanks of that bracket (run 31886247498).

\emph{Limitations.} Hamiltonian-Hopf wording is authorized only at this exact bracket, not at neighboring catalog rows. Flank spectra are evaluated at a fixed +/-5e-6 offset in m2, not as a limit at the critical point. A sign-change bracket at 60 digits localizes a root of the computed event function; it is not an interval-arithmetic existence proof and does not separately certify the collision's nondegeneracy.

\paragraph{Three Mixed Organizers.}
Near each of the three coarse +1/-1 mechanism switches visible on the published 0.001 grid there is a root of the simultaneous conditions G+=G-=0, that is, a preimage of the universal spectral vertex (alpha,beta)=(4,4): principal-left (0.9292391921, 0.8853664625), secondary-left (0.9967681995, 0.9560193602), principal-right (1.0495531867, 1.1294758073). Each root was found by float64 mass-eliminated Newton and then recomputed at 60 digits by an independent Julia BigFloat mass Newton with canonical Jacobi monodromy and physical E\^{}omega/E classification.

\emph{Method.} Float64 mass-eliminated Newton for G+=G-=0, then independent Julia BigFloat mass Newton and physical E\^{}omega/E classification (run 31886247456).

\emph{Limitations.} 0-D roots, not yet event-arc germs. A possible fourth mixed organizer at the secondary-right death is not claimed. The three frozen artifacts declare themselves at the 'high\_precision\_supported' rung of the evidence ladder, not at 'independently\_reproduced'; only the two headline events reach the higher rung. This claim must not be worded as an independent reproduction. Each root is localized by Newton residual (relative event norm of order 1e-14), not by a sign-change bracket or interval arithmetic. Identifying a coarse grid switch with the nearby root is a proximity argument at the 0.001 grid resolution. The assembler binds continuation germs within GERM\_ATTACH\_DISTANCE = 0.008 in mass; measured on the release configuration that threshold reproduces the committed six-attachment assembly only on the half-open window [0.006837, 0.009431), 1.379x wide (largest accepted attachment 0.006837449, nearest rejected mode-matching candidate 0.009430577). Re-derive with scripts/audit\_germ\_attachment\_window.py. GERM\_ATTACH\_DISTANCE does double duty: it caps how far a germ may sit from its organizer AND how far an edge endpoint may sit from that germ. Those compose, so the effective organizer-to-endpoint reach is up to 0.016 -- twice the number the constant appears to state, and 16 grid steps. The germ step is not uniform across the three organizers. mixed\_secondary\_left was traced at --step 0.003 rather than the 7.5e-4 default (.github/workflows/headline-mixed-germs.yml), chosen specifically so its minus\_one germ would travel far enough to bring cell 392's endpoint -- 8.59e-3 from the organizer -- inside the attachment radius. The check that keeps this honest: it did not also reach the secondary\_left\_birth blocker, whose endpoint (minus\_one\_s\_to\_u\_0 start) remains 2.81e-2 from the organizer and 2.6379e-2 from the nearest germ, past even the composed 0.016 reach.

\paragraph{Known limitations of this release.}
\begin{itemize}
\item This is not a solution of the general three-body problem. Every result here is scoped to one published catalog, one declared gauge, and planar linear Floquet stability.
\item The mechanism-resolved critical graph is not finished. SOLVED is forbidden.
\item v1 'stable' means planar linear Floquet stability only: not nonlinear stability, not KAM stability, not spatial (non-planar) stability.
\item Gate A pass is scoped to mechanism words the paper is allowed to use. Fold, fourth mixed, and daughter labels remain unauthorized.
\item The one-family statement is a numerical continuation certificate for the declared catalog domain.
\item Negative literature searches are not a novelty theorem, and no priority claim ('first', 'the first') is authorized.
\item 'Completeness' is bounded, and its bound is small. The frozen stability-neck raster (research/evidence/V1\_NECK\_RASTER\_2026-08-16.json) covers m1 in [0.997,0.999] x m2 in [0.993,1.012] at step 1e-4 -- 4011 corrected samples, all 21 lines 'separated', interior\_merge 0, truncation\_undecidable 0, minimum resolved unstable gap 3.0e-4, maximum shooting residual 4.7807e-10. That rectangle is 0.0253\% of the declared domain m1 in [0.8,1.1] x m2 in [0.7,1.2]. Derive the figure with scripts/completeness\_scope.py, which reads the rectangle from the MERGE job of .github/workflows/stability-neck-scan.yml, cross-checks it against the committed raster artifact, and takes the domain from DECLARED\_DOMAIN in scripts/assemble\_critical\_graph.py.
\item The other completeness input is not a sample of the domain outside that rectangle. All 12 proposals in research/evidence/V1\_AL\_POCKET\_SCREEN\_2026-08-15.json lie in ONE pocket at the principal lower transition, m1 in [0.80005, 0.80276] x m2 in [0.75266, 0.75407] -- an area fraction of 2.539e-5, i.e. 0.002539\% of the declared domain -- and that artifact declares itself 'AI proposals plus float64 screening only; not scientific discovery evidence'. Outside the neck rectangle and that pocket, completeness rests on the published 0.001 grid alone. Both facts must travel with the screen wherever it is cited as completeness support, because it is one of the two inputs to the frozen certificate; they are now carried by the certificate's own scope description too (research/evidence/V1\_COMPLETENESS\_CERTIFICATE\_2026-08-16.json, active\_learning.extent and note), which src/threebody\_atlas/completeness.py emits for every future freeze.
\item The 620/620 critical-root census clears the frozen event gate with little room: maximum |event| is 1.9898e-8 against the frozen gate of 2e-8, a margin of about 0.5 percent, and 165 of the 620 roots exceed 1e-8. Verify from research/evidence/V1\_HYBRID\_CRITICAL\_ROOTS\_2026-08-15.json.
\item A float64 re-evaluation of a published organizer chart is not reproducible and must never be used as a gate check. Re-evaluating the three canonical 60-digit organizer charts with the repository's float64 stack gives plus\_one events that vary strongly with the machine: principal-left about -1.28e-7 to -1.35e-7, secondary-left about -1.96e-8 to -2.30e-8, and principal-right about -2.56e-8 to -8.66e-8, the last a factor of 3.4 spread measured across macOS arm64 and two different x86\_64 Linux machines. Secondary-left's range straddles the frozen 2e-8 gate, so that organizer clears it on one machine and misses it on another. A quantity moving that much under a change of libm, BLAS or CPU is dominated by accumulated float64 integration error rather than by the dynamics. This is a limitation of float64 evaluation, not of the organizers: at 60 digits the same charts give relative plus\_one events of order 1e-14. Any float64 reproduction of an organizer must therefore be re-corrected, never merely re-evaluated.
\item The assembled graph is not yet one connected object. Taking research/evidence/V1\_CRITICAL\_GRAPH.json as committed, the edge incidence yields three components among the edge-incident nodes ({domain\_m1\_min\_m2\_0p756, mixed\_principal\_left, mixed\_principal\_right, domain\_m2\_max\_m1\_1p071}, {mixed\_secondary\_left, secondary\_right\_death} and {domain\_m1\_min\_m2\_0p761, domain\_m2\_max\_m1\_1p053}), because two edge ends are still unclassified; four further declared nodes (both headline event representatives, secondary\_left\_birth, the daughter) have no edge incidence at all, and secondary\_left\_birth is the only unexplained node. Continuation-connectedness of the family sheet (Gate C) is a different statement from connectedness of this critical graph, and does not imply it.
\item Germ attachment is threshold-dependent, and the threshold does double duty. The assembler binds an edge end to a mixed organizer when the mass distance is at most GERM\_ATTACH\_DISTANCE = 0.008, and the same constant caps how far a germ may sit from its own organizer, so the effective organizer-to-endpoint reach is up to 0.016. Auditing the release configuration shows the committed six-attachment assembly keeps every attachment it has only for thresholds on the half-open window [0.006837, 0.009431), 1.379x wide: the largest accepted attachment is 0.006837449 and the nearest rejected mode-matching candidate is 0.009430577. The assembled graph does not visibly change until 0.010349059 (1.514x), because that rejected candidate's endpoint is already bound by its own classification artifact. Re-derive with scripts/audit\_germ\_attachment\_window.py.
\item One germ trace was tuned to reach an endpoint. The mixed\_secondary\_left germs were traced at --step 0.003 instead of the 7.5e-4 default, chosen so the minus\_one germ would travel far enough to bring cell 392's endpoint (8.59e-3 from the organizer) inside the attachment radius. The check that keeps that honest is that it did not also sweep up the blocked endpoint: minus\_one\_s\_to\_u\_0's start is 2.81e-2 from the organizer and 2.6379e-2 from the nearest germ, beyond even the composed 0.016 reach, so secondary\_left\_birth stays unresolved.
\item The novelty audit is stale against Gate D's own rule. Gate D demands a same-day literature search; the manifest records last\_search\_date 2026-08-15 with novelty.status 'pending'. See research/NOVELTY\_AUDIT.md for what must be refreshed, and by whom, before any solved release.
\end{itemize}

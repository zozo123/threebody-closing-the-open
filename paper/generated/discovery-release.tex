% Generated from research/DISCOVERY_RELEASE.json.
\subsection*{Machine-readable discovery status}
Scientific status: \textbf{OPEN}. OPEN. Gate C passed: the frozen catalog is supported as one continuation-connected component and the 2023 two-set reading rests on a projection we show to be folded and non-injective. Gate A passed for the three mechanism words the paper is allowed to use: a bracketed physical +1 crossing, a bracketed opposite-Krein Hamiltonian-Hopf collision, and three (4,4) mixed roots. Every one of the 620 published adjacent S/U cells has been localized once and assigned to one of seven catalog mechanism polylines, and those seven are now joined by six certified label-invisible event-sign polylines from the 2026-08-16 sweep. Sixteen mixed continuation germs are frozen (four for each of the three headline organizers plus four the retained secondary-right node owes in its own right). Finite event-sign lattice ends are left unclassified; they are not interior\_lattice\_terminus nodes. Three such ends remain. plus\_one\_sweep\_component\_12 high is bound to mixed\_principal\_right by a certified variational step. The collision curve is still its own component, and the 2026-08-17 full-domain sweep still reports additional uningested components. Completeness IS now frozen, but only in the bounded sense the known\_limitations spell out: a 0.0253\% neck rectangle plus one 0.002539\% off-grid pocket. Gate B and Gate D remain pending, so SOLVED is forbidden.
\paragraph{What this release would establish.}
If and only if every gate closes, v1 would establish the following, and nothing wider. Within the frozen Li--Li--Liao unequal-mass non-hierarchical catalog on the declared domain m1 in [0.8,1.1], m2 in [0.7,1.2] at m3 = 1, and under one declared normalization and continuation gauge, the catalog behaves as a single continuation-connected sheet rather than the two families a scale-invariant projection appears to show; and on that sheet the planar linear-Floquet stability boundary can be decomposed into mechanism-specific arcs (+1 crossings, -1 crossings, and trace collisions) that meet at a small number of mixed (4,4) organizers, with representative events reproduced by an independent arbitrary-precision implementation and physical symplectic-quotient diagnostics rather than by deletion of trivial multipliers. That is a finite, numerical, resolution-bounded statement about one published catalog under one gauge. It is not a theorem, not a statement about the general three-body problem, not a statement about nonlinear or KAM stability, not a statement about spatial (non-planar) stability, and not a claim of priority.
\paragraph{Gates.}
\begin{itemize}
\item Gate A --- Independent truth for every mechanism word used in the paper: \textbf{PASS}. A +1, -1, mixed, or Hamiltonian-Hopf/Krein label may appear as an ATLAS result only after independent BigFloat correction, event localization, and physical symplectic-quotient diagnostics at the exact bracket. The paper currently uses only the bound +1, Hamiltonian-Hopf, and three mixed organizers.
\item Gate B --- Complete mechanism-resolved Floquet critical graph on the connected family sheet: \textbf{PENDING}. Every catalog S/U cell belongs to exactly one mechanism-specific polyline; N edges need not equal 620; every endpoint is a domain boundary, mixed organizer, or verified fold; mixed germs come from continuation artifacts; Newton-failed is forbidden; completeness is frozen; V1\_CRITICAL\_GRAPH.json has release\_ready true.
\item Gate C --- One continuation-connected family: \textbf{PASS}. Family identity is bidirectional continuation in the Li chart and a generic strict-periodic chart, after rank and worst-MST attacks. Projected (T,L) branches are not families.
\item Gate D --- Solved-release freeze: \textbf{PENDING}. Assembler release\_ready, same-day novelty, manuscript generated only from release\_claim records, --require-solved on the tagged commit.
\end{itemize}
\paragraph{Authorized release claims.}
\begin{itemize}
\item \textbf{one-continuation-family}. Within the frozen Li--Li--Liao catalog and the declared v1 gauge, the evidence supports a single continuation-connected component: the 135,445-row mass-grid adjacency graph is connected (270,087 adjacency edges), and every one of the 26 adversarially selected links that could have separated it was crossed bidirectionally by continuation in the Li chart. Chart-independence was tested on one of those 26 -- the pathological invariant-near-duplicate bridge -- which was repeated in a generic translation-reduced strict-periodic chart outside the Li ansatz; the other 25 were not repeated in a second chart. Explicit absence of branch hysteresis is recorded for the 5 macroscopic bottlenecks. No surviving rank-loss, directionality, chart-dependence, or worst-MST obstruction to connectivity was found. The competing two-set reading is a reading of the scale-invariant (T,L) projection, which on this sheet is demonstrably folded and non-injective, so two visible branches there do not by themselves establish two dynamical families. \emph{Limitations.} Numerical continuation statement for the declared catalog domain, not a theorem about the full planar three-body moduli space. Continuation was certified on 26 distinct adversarially chosen links (5 macroscopic bottlenecks, 1 invariant-near-duplicate bridge, the 20 globally worst MST chart jumps), not on all 135,444 MST edges; connectivity across the remainder rests on 0.001-grid adjacency plus the absence of any surviving obstruction. The bridge is one link crossed in two charts and must not be counted as two links. Chart-independence is demonstrated on exactly one of the 26 links. Per research/V1\_CONNECTIVITY\_CERTIFICATE\_2026-08-15.md item 4, only the pathological invariant-near-duplicate bridge was repeated in a generic strict-periodic chart; the 5 macroscopic bottlenecks and the 20 worst MST edges are Li-chart results. 'Without branch hysteresis' is recorded only for the 5 bottlenecks (certificate item 2); it is not asserted for the other 21 links. A negative adversarial search cannot exclude a separation finer than the 0.001 catalog grid, or outside the declared domain m1 in [0.8,1.1], m2 in [0.7,1.2] at m3=1. 'One component' is the decomposition best supported by this evidence under this gauge. It is not a disproof of the 2023 analysis on its own (T,L) terms, and it is not a statement about families of the general three-body problem.
\item \textbf{principal-lower-plus-one}. On the slice m1=0.8023446113666945 with m3=1, the lower stability change of the sheet is bracketed at m2 in [0.7540187211922449, 0.7540187227698909] (width 1.58e-9) by a sign change of the event function, and is independently reproduced in a separate Julia BigFloat stack, with canonical Jacobi monodromy and physical E\^{}omega/E diagnostics, as a physical +1 crossing of the reduced return map. Across that bracket the smaller-m2 flank carries a real reciprocal pair off the unit circle and the larger-m2 flank is elliptic. \emph{Limitations.} Planar linear Floquet only. One bracketed representative on one m1 slice; it does not certify the mechanism along the rest of the +1 edge. Flank spectra are evaluated at a fixed +/-5e-6 offset in m2, not as a limit at the critical point. A sign-change bracket at 60 digits localizes a root of the computed event function; it is not an interval-arithmetic existence proof and does not separately certify transversality.
\item \textbf{principal-upper-hamiltonian-hopf}. On the slice m1=0.8022889780964406 with m3=1, the upper stability change of the sheet is bracketed at m2 in [0.7592347034247101, 0.7592347096924873] (width 6.27e-9) by a sign change of the event function, and is independently reproduced in a separate Julia BigFloat stack as an opposite-Krein Hamiltonian-Hopf collision. The stable flank has two upper-half unit modes of opposite Krein sign; the unstable flank opens a reciprocal complex quartet (radius deviation 1.83e-2). \emph{Limitations.} Hamiltonian-Hopf wording is authorized only at this exact bracket, not at neighboring catalog rows. Flank spectra are evaluated at a fixed +/-5e-6 offset in m2, not as a limit at the critical point. A sign-change bracket at 60 digits localizes a root of the computed event function; it is not an interval-arithmetic existence proof and does not separately certify the collision's nondegeneracy.
\item \textbf{three-mixed-organizers}. Near each of the three coarse +1/-1 mechanism switches visible on the published 0.001 grid there is a root of the simultaneous conditions G+=G-=0, that is, a preimage of the universal spectral vertex (alpha,beta)=(4,4): principal-left (0.9292391921, 0.8853664625), secondary-left (0.9967681995, 0.9560193602), principal-right (1.0495531867, 1.1294758073). Each root was found by float64 mass-eliminated Newton and then recomputed at 60 digits by an independent Julia BigFloat mass Newton with canonical Jacobi monodromy and physical E\^{}omega/E classification. \emph{Limitations.} 0-D roots, not yet event-arc germs. A possible fourth mixed organizer at the secondary-right death is not claimed. The three frozen artifacts declare themselves at the 'high\_precision\_supported' rung of the evidence ladder, not at 'independently\_reproduced'; only the two headline events reach the higher rung. This claim must not be worded as an independent reproduction. Each root is localized by Newton residual (relative event norm of order 1e-14), not by a sign-change bracket or interval arithmetic. Identifying a coarse grid switch with the nearby root is a proximity argument at the 0.001 grid resolution. The assembler binds continuation germs within GERM\_ATTACH\_DISTANCE = 0.008 in mass; measured on the release configuration that threshold reproduces the committed attachment set only on the half-open window [0.007026, 0.008251), 1.174x wide (largest accepted attachment 0.007236997, nearest rejected mode-matching candidate 0.008250801, both on plus\_one\_sweep\_component\_12). Re-derive with scripts/audit\_germ\_attachment\_window.py. GERM\_ATTACH\_DISTANCE does double duty: it caps how far a germ may sit from its organizer AND how far an edge endpoint may sit from that germ. Those compose, so the effective organizer-to-endpoint reach is up to 0.016 -- twice the number the constant appears to state, and 16 grid steps. The germ step is not uniform across the three organizers. mixed\_secondary\_left was traced at --step 0.003 rather than the 7.5e-4 default (.github/workflows/headline-mixed-germs.yml), chosen specifically so its minus\_one germ would travel far enough to bring cell 392's endpoint -- 8.59e-3 from the organizer -- inside the attachment radius. The check that keeps this honest: it did not also reach the secondary\_left\_birth blocker, whose endpoint (minus\_one\_s\_to\_u\_0 start) remains 2.81e-2 from the organizer and 2.6379e-2 from the nearest germ, past even the composed 0.016 reach.
\end{itemize}
\paragraph{Known limitations.}
\begin{itemize}
\item This is not a solution of the general three-body problem. Every result here is scoped to one published catalog, one declared gauge, and planar linear Floquet stability.
\item The mechanism-resolved critical graph is not finished. SOLVED is forbidden.
\item v1 'stable' means planar linear Floquet stability only: not nonlinear stability, not KAM stability, not spatial (non-planar) stability.
\item Gate A pass is scoped to mechanism words the paper is allowed to use. Fold, fourth mixed, and daughter labels remain unauthorized.
\item The one-family statement is a numerical continuation certificate for the declared catalog domain.
\item Negative literature searches are not a novelty theorem, and no priority claim ('first', 'the first') is authorized.
\item 'Completeness' is bounded, and its bound is small. The frozen stability-neck raster (research/evidence/V1\_NECK\_RASTER\_2026-08-16.json) covers m1 in [0.997,0.999] x m2 in [0.993,1.012] at step 1e-4 -- 4011 corrected samples, all 21 lines 'separated', interior\_merge 0, truncation\_undecidable 0, minimum resolved unstable gap 3.0e-4, maximum shooting residual 4.7807e-10. That rectangle is 0.0253\% of the declared domain m1 in [0.8,1.1] x m2 in [0.7,1.2]. Derive the figure with scripts/completeness\_scope.py, which reads the rectangle from the MERGE job of .github/workflows/stability-neck-scan.yml, cross-checks it against the committed raster artifact, and takes the domain from DECLARED\_DOMAIN in scripts/assemble\_critical\_graph.py.
\item The other completeness input is not a sample of the domain outside that rectangle. All 12 proposals in research/evidence/V1\_AL\_POCKET\_SCREEN\_2026-08-15.json lie in ONE pocket at the principal lower transition, m1 in [0.80005, 0.80276] x m2 in [0.75266, 0.75407] -- an area fraction of 2.539e-5, i.e. 0.002539\% of the declared domain -- and that artifact declares itself 'AI proposals plus float64 screening only; not scientific discovery evidence'. Outside the neck rectangle and that pocket, completeness rests on the published 0.001 grid alone. Both facts must travel with the screen wherever it is cited as completeness support, because it is one of the two inputs to the frozen certificate; they are now carried by the certificate's own scope description too (research/evidence/V1\_COMPLETENESS\_CERTIFICATE\_2026-08-16.json, active\_learning.extent and note), which src/threebody\_atlas/completeness.py emits for every future freeze.
\item The 620/620 critical-root census clears the frozen event gate with little room: maximum |event| is 1.9898e-8 against the frozen gate of 2e-8, a margin of about 0.5 percent, and 165 of the 620 roots exceed 1e-8. Verify from research/evidence/V1\_HYBRID\_CRITICAL\_ROOTS\_2026-08-15.json.
\item A float64 re-evaluation of a published organizer chart is not reproducible and must never be used as a gate check. Re-evaluating the three canonical 60-digit organizer charts with the repository's float64 stack gives plus\_one events that vary strongly with the machine: principal-left about -1.28e-7 to -1.35e-7, secondary-left about -1.96e-8 to -2.30e-8, and principal-right about -2.56e-8 to -8.66e-8, the last a factor of 3.4 spread measured across macOS arm64 and two different x86\_64 Linux machines. Secondary-left's range straddles the frozen 2e-8 gate, so that organizer clears it on one machine and misses it on another. A quantity moving that much under a change of libm, BLAS or CPU is dominated by accumulated float64 integration error rather than by the dynamics. This is a limitation of float64 evaluation, not of the organizers: at 60 digits the same charts give relative plus\_one events of order 1e-14. Any float64 reproduction of an organizer must therefore be re-corrected, never merely re-evaluated.
\item The assembled graph is not yet one connected object, and it is not the complete critical set. Taking research/evidence/V1\_CRITICAL\_GRAPH.json as committed, there are 13 edges (7 catalog plus 6 certified event-sign sweep polylines) and 14 nodes. TWO finite-lattice sweep ends are left unclassified rather than manufactured into interior\_lattice\_terminus nodes: minus\_one\_sweep\_component\_0 start (0.892, 0.753) and minus\_one\_sweep\_component\_1 end (1.042, 0.858). It was three until plus\_one component 12's low tip was re-localized at BigFloat dps=60: the stored float64 cell 10131 sat 5.21e-7 off the zero set in m2, so it failed re-correction and both of that component's sides collapsed onto the one remaining certifying seed. The corrected cell re-certifies under float64, giving the component two distinct seeds, and its two termini then resolve to DIFFERENT organizers -- secondary\_right\_death at the low end and mixed\_principal\_right at the high end. Its 21 gate-passing continuation points are now committed as edge vertices, which also closed the one missing\_critical\_curve an independent 35-scan-line sign-topology audit had found at m1 1.045: that was this same arc, outside an edge whose m1 span stopped at 1.046. The two remaining ends are BigFloat-verified roots (|event| 6.7e-10 and 2.3e-9, closure \textasciitilde{}1e-26) that float64 cannot re-certify, and outward continuation past them returns event=inf. Among classified incident nodes the edge incidence yields two components ({domain\_m1\_min\_m2\_0p761, domain\_m2\_max\_m1\_1p053} and the remaining classified organizers and domain exits, including the new faces domain\_m1\_max\_m2\_1p092 and domain\_m2\_max\_m1\_1p094); three further declared nodes (both headline event representatives and the daughter) have no edge incidence at all. There are no unexplained nodes. release\_ready is held false by those unclassified lattice ends and by the sign\_topology\_clean conjunct (the assembler still reads the 2026-08-16 audits, which predate these sweep polylines). The 2026-08-17 full-domain sweep further reports components absent from even this 13-edge graph. Continuation-connectedness of the family sheet (Gate C) is a different statement from connectedness of this critical graph, and does not imply it.
\item Germ attachment is threshold-dependent, and the threshold does double duty. The assembler binds an edge end to a mixed organizer when the mass distance is at most GERM\_ATTACH\_DISTANCE = 0.008, and the same constant caps how far a germ may sit from its own organizer, so the effective organizer-to-endpoint reach is up to 0.016. Auditing the release configuration shows the committed twelve-attachment assembly keeps every attachment it has only for thresholds on the half-open window [0.007026, 0.008251), 1.174x wide: the largest accepted attachment is 0.007236997 (plus\_one\_sweep\_component\_12 end <-> mixed\_principal\_right) and the nearest rejected mode-matching candidate is 0.008250801 (plus\_one\_sweep\_component\_12 start <-> secondary\_right\_death). That window NARROWED from 1.303x when plus\_one component 12's continuation arc was ingested, and 0.008 now sits only 3.0\% below the nearest rejected candidate rather than 15\%. The narrowing is benign for the topology but must be stated: the near-threshold candidate is another germ of secondary\_right\_death, the SAME organizer that endpoint is already bound to, so admitting it would add an attachment without moving any endpoint's node. A future candidate this close to the threshold on a DIFFERENT organizer would not be benign. Re-derive with scripts/audit\_germ\_attachment\_window.py.
\item One germ trace was tuned to reach an endpoint. The mixed\_secondary\_left germs were traced at --step 0.003 instead of the 7.5e-4 default, chosen so the minus\_one germ would travel far enough to bring cell 392's endpoint (8.59e-3 from the organizer) inside the attachment radius. The check that keeps that honest is that it did not also sweep up the blocked endpoint: minus\_one\_s\_to\_u\_0's start is 2.81e-2 from the organizer and 2.6379e-2 from the nearest germ, beyond even the composed 0.016 reach, so secondary\_left\_birth stays unresolved.
\item The novelty audit is stale against Gate D's own rule. Gate D demands a same-day literature search; the manifest records last\_search\_date 2026-08-15 with novelty.status 'pending'. See research/NOVELTY\_AUDIT.md for what must be refreshed, and by whom, before any solved release.
\item Negative literature searches are not a novelty theorem.
\item The 620-cell census passes the frozen 2e-8 event gate by 0.511\%, not comfortably. Maximum |event| is 1.9897980818583960e-08 = 99.489\% of the gate (research/evidence/V1\_HYBRID\_CRITICAL\_ROOTS\_2026-08-15.json); 165 of 620 roots exceed 1e-8 and 7 exceed 95\% of the gate. No sentence may report that the census 'passes the 2e-8 gate' without reporting that margin.
\item That margin is smaller than the uncertainty of the quantity it constrains. Re-evaluating the event functional at the exact recorded state, with the locked numpy 2.5.2 / scipy 1.18.0, across integrator tolerances rtol 5e-13 to 5e-14 moves it by a median 1.48e-7 -- 7.4x the gate -- and 20 of 24 probed cells (worst offenders and evenly spaced controls alike) cross 2e-8 at some tight tolerance (research/evidence/V1\_EVENT\_MARGIN\_AUDIT\_2026-08-16.json). The float64 lane's accepted residuals sit below its own evaluation noise floor for the \textasciitilde{}165 cells in the top decade, so |event| <= 2e-8 certifies the localizer's stopping rule, not the distance to the true root.
\item The float64 residuals are not reproducible across CPU architectures at gate scale. 'reevaluated\_at\_accepting\_tolerance' is bit-for-bit the computation that produced each recorded float64 residual, on the recorded state, and reproduces every digit when re-run on one machine; against the frozen CI values it differs by a median 2.78e-8 (1.39x the gate) and a maximum 4.14e-7 (20.7x the gate), and 10 of 22 comparable probes land above 2e-8. The cause is the \textasciitilde{}8-digit cancellation in the event functionals (Delta = (alpha-4)\^{}2 - 4(beta - 4 alpha + 8) with alpha, beta of order 1) sitting on top of architecture-dependent BLAS summation inside the monodromy product.
\item The independent Julia BigFloat lane does not escape this: its localizer also terminates on the first iterate below 2e-8, so 7 of its 158 roots are recorded at 1e-8 to 1.66e-8 despite dps=40 arithmetic capable of 1e-30. Those seven are gate-limited, not precision-limited.
\item The worst cells scatter rather than cluster, which is the signature of a measurement floor and not of physics: among the 25 worst, event-mode enrichment is 1.27/1.00/0.59, orientation enrichment 1.07/0.93, twelve distinct m1 bands, Spearman correlation of |event| with m1 of 0.12, and median organizer distance 0.054 against a census median of 0.049. The floor is quantified: the monodromy invariants alpha and beta are themselves reproducible only to a median relative 4.2e-9 across the tolerance ladder, and the event functionals cancel by a median factor 2.6e8 (worst 5.2e10).
\item Pulling max |event| down 10x to 2e-9 is impossible in the float64 lane at any integrator tolerance -- the target is 73x below that lane's own evaluation uncertainty -- and requires routing all 620 cells through the BigFloat lane at dps=40 with a 2e-9 stopping tolerance. Measured cost: mean 5.45 secant iterations per cell over 204 recorded BigFloat cells, \textasciitilde{}23 min per cell at the CI-measured correction cost, \textasciitilde{}179 serial CPU-hours, under 3 wall-hours at the existing 62-way fan-out.
\item The census root locations are far more robust than the census certificate. With the measured d(event)/d(m2) of 20 to 660 per unit mass, even the full evaluation uncertainty maps to an m2 uncertainty of 1e-10 to 8e-9, well inside the 1e-3 published cells. The topology of the graph is not in question; the numerical certificate attached to it is.
\item Sign-topology completeness is now supported by an INDEPENDENT audit rather than by the 2026-08-16 audits, which were computed against the seven-edge catalog graph and cannot be evaluated against the current thirteen-edge graph at all (edges\_from\_graph resolves edge cell\_ids against the roots file, and the sweep edges' cell ids 10000-10133 are absent from the 620-root census, so it raises KeyError). The replacement audits probe 35 m1 scan lines excluding the seven that every previously committed audit used -- lines the edge set was never fitted to -- plus the fine crossing configuration: 465 probes, 365 converged, 13 edges, zero violations, both reading research/evidence/V1\_COMBINED\_CRITICAL\_ROOTS\_2026-08-17.json so both re-derive from the repository alone. That audit earned its verdict: on first run it reported one missing\_critical\_curve, plus\_one at m1 1.045 refined to m2 [1.0961009, 1.0963566], which proved to be plus\_one component 12's own arc lying outside an edge whose m1 span stopped at 1.046; ingesting the arc's 21 gate-passing points closed it. REACH LIMIT: 100 of 465 probes fail, each with a closure norm of order 1, meaning no periodic orbit closes at that mass point; they cluster below m2 \textasciitilde{} 0.80 at the family's existence boundary, so the audit certifies sign topology only where the family exists. release\_ready remains false on the two unresolved lattice ends alone.
\item Sign-topology completeness is NOT established, and the flag that said otherwise has been withdrawn. sign\_topology\_clean was true on research/evidence/V1\_SIGN\_TOPOLOGY\_AUDIT\_INDEPENDENT\_35LINE\_2026-08-17.json, which reports zero violations over 35 scan lines. Measured afterwards, that artifact had 100 of its 465 probes FAIL -- 97 of them closure failures with residuals between 3.9e-2 and 3.8e0 against the 1e-7 gate -- and its converged probes covered a mean 47.4\% of the declared m2 span, as little as 9.1\% on one line, with a widest inter-probe gap of 0.4101 against a 0.001 mass grid. scripts/audit\_sign\_topology.py drops every probe whose orbit fails to close, so an interval where the shooting solve fails contributes no probes, produces no sign change, and is reported identically to an interval that was measured and found empty. The audit script says so itself in its own scope field -- it falsifies completeness where it fires and cannot certify completeness where it does not -- but nothing machine-checked that disclaimer: sign\_topology\_report read only violation\_counts, so a two-key file containing {"violation\_counts": {}} and no probes at all passed as clean, as did this project's own test fixture carrying probe\_count 0 and the note SYNTHETIC FIXTURE, NOT EVIDENCE. The conjunct now also requires an audit to demonstrate coverage: every gap between adjacent CONVERGED probes must be at most the 0.001 mass grid step, since two critical curves are resolved as distinct objects only when they sit at least one grid step apart. CONSEQUENCE AND OPEN WORK. An earlier version of this limitation claimed the orbit family does not exist below m2 \textasciitilde{} 0.80, inferring a physical boundary from probes that failed with closure norms of order 1. THAT WAS WRONG, and it was an artifact of probe density. A calibration sweep on 2026-08-17 ran the same two scan lines at max-gap 0.070, 0.040, 0.020, 0.010 and 0.005: convergence went 9/10 at 0.070 to 98/98 at 0.005, with ZERO failures at the finest setting and a converged m2 hull of 0.7049..1.1950, i.e. essentially the whole declared span, including 20 converged probes below m2 0.80. Probe failure is a function of probe spacing, not of where the family exists, because each probe is seeded from its neighbours. The same sweep fixes the instrument's detection threshold: a real minus\_one curve at m1 0.889 is INVISIBLE at max-gap 0.070 and 0.040 and detected at 0.020, 0.010 and 0.005, with the bracket tightening monotonically 0.736312..0.754467 -> 0.739105..0.748881 -> 0.739105..0.743993. Every committed audit used the 0.070 default, so every one was blind by a factor of two to four. The control line m1 0.900 stayed clean at all five settings, so the detector has no false-positive mode at these resolutions. Full coverage is therefore ACHIEVABLE, and earning sign\_topology\_clean is a compute problem at a calibrated resolution rather than a blocked scientific one.
\item RESOLVED 2026-08-18: Every file the canonical invocation (scripts/assemble\_v1\_critical\_graph.sh) consumes is registered above with an assembler\_* role. Before 2026-08-18 the registry and the gate inputs were disjoint except for the graph itself (overlap 1 of 20 registered / 16 consumed -- issue \#212 section 2); a registry that does not name what the gate actually reads cannot support an outside audit of the claim.
\end{itemize}
\paragraph{Novelty status.}
PENDING; last literature search 2026-08-15, maximum permitted age 7 days. A negative literature search is not a proof of priority.
\paragraph{Remaining blockers.}
\begin{itemize}
\item Classify the secondary-left G- birth (\#110).
\item Classify the secondary-right death without claiming a fourth organizer until it is bound (\#111 / \#127).
\item Classify the lower +1 daughter, including no\_branch\_attachment as a valid close.
\item Completeness is now frozen as a sealed, source-verified certificate (research/evidence/V1\_COMPLETENESS\_CERTIFICATE\_2026-08-16.json, passed true, zero verification errors) over the widened neck raster; what remains is to keep release\_ready flippable only by the assembler (\#116 / \#128), never by hand.
\item Classify the two unclassified edge ends so the assembled graph's edge incidence is one connected object rather than three components plus four unattached nodes.
\item Refresh the novelty search on the actual freeze day and record its outcome; the recorded search date is 2026-08-15 and Gate D requires same-day.
\item Then \#118: same-day novelty, manuscript from release\_claim records, --require-solved.
\end{itemize}

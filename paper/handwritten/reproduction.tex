% HAND-WRITTEN prose, not generator output.  It used to sit in paper/generated/
% under a header claiming it was generated, while no script produced it; that
% made an unaudited hand-editable file look machine-authored.  It lives here
% instead, and scripts/check_manuscript_claims.py scans it exactly like main.tex:
% every sentence is locked in paper/CLAIM_LOCK.json and screened for ungated
% assertions.  Numbers quoted here must be traceable to research/evidence/.
The upstream supplementary file is rejected unless its Git blob is exactly
{\scriptsize\texttt{79b2963df43e62201c35690bfc22bec166132427}}.
The first independent reconstruction of the published initial-condition
convention matched the published S/U labels on the first inspected rows,
including both sides of both published transitions at $m_1=0.8$, $m_3=1$.
That is a screening reproduction of a literature fact, not an ATLAS discovery.

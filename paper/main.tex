\documentclass[11pt]{article}
\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb,booktabs,graphicx,hyperref}
\title{One continuation family, three mixed organizers, and two bound Floquet events\\
in the Li--Li--Liao unequal-mass catalog\\
\large (claim-gated atlas; scientific status OPEN)}
\author{Yossi Eliaz}
\date{\today}

\begin{document}
\maketitle

\begin{abstract}
The Li--Li--Liao 2021 catalog reports $135{,}445$ unequal-mass non-hierarchical
periodic orbits and $13{,}315$ linearly stable samples on a $0.001$ mass grid.
A 2023 analysis of the same table argued that the set is two families.
We treat family identity as continuation connectivity, not as a plot of
scale-invariant invariants, and we treat the linear-stability boundary as the
preimage of a universal Floquet domain rather than as a rectangular S/U mask.

\textbf{Earned results.}
(i)~The frozen catalog is supported as one continuation-connected component in
the declared gauge; each of the $26$ adversarially chosen links that could have
separated it was crossed bidirectionally in the Li chart, and one of them --- the
pathological invariant-near-duplicate bridge --- was crossed again in a generic
strict-periodic chart.
(ii)~On the slice $m_1\approx0.80234$ the lower transition is an independently
reproduced physical $+1$ crossing, bracketed to $m_2$ width
$1.58\times10^{-9}$.
(iii)~On the slice $m_1\approx0.80229$ the upper transition is an independently
reproduced opposite-Krein Hamiltonian--Hopf event, bracketed to $m_2$ width
$6.27\times10^{-9}$.
(iv)~Near each of the three coarse $+1/-1$ switches there is a root of
$G_+=G_-=0$, recomputed at $60$ digits in an independent stack; those three
artifacts sit at the \emph{high-precision supported} rung of the evidence
ladder, one rung below the two bracketed events.

\textbf{Not earned.}
The connected critical graph is unfinished.
All $620$ published S/U cells are now localized once and assigned to seven
mechanism polylines, but two edge ends remain unclassified, so the assembled
edge incidence is still three components rather than one, and the
secondary-lobe birth remains unexplained.
Completeness is frozen only in the bounded sense quantified below.
Scientific status remains OPEN.
This is not a solution of the general three-body problem, and ``stable'' here
means planar linear Floquet stability only --- not nonlinear stability, not
KAM stability, and not spatial stability.
No priority claim is made.
\end{abstract}

\section{The problem, and what it is not}

The general Newtonian three-body problem is not being solved.
The v1 question is finite \cite{li2021stable,stancevic2023moth,li2025review}:

\begin{quote}
Is the Li--Li--Liao unequal-mass non-hierarchical catalog one
continuation-connected family or several?
On each component, what is the planar linear-Floquet critical graph in the
$(m_1,m_2)$ plane at $m_3=1$?
\end{quote}

Li, Li and Liao continued one family on $m_3=1$, $m_1\in[0.8,1.1]$,
$m_2\in[0.7,1.2]$, spacing $0.001$, and plotted a stable domain
\cite{li2021stable}.
Those $135{,}445$ orbits and $13{,}315$ stable labels are literature facts.
ATLAS may not claim them.

Stan\v{c}evi\'c, Vasiljevi\'c and Dmitra\v{s}inovi\'c argued that the same
table is two independent sets, using mass-corrected period and angular-momentum
relations \cite{stancevic2023moth}.
That is the sharpest competing reading of the exact catalog.
We accept their diagnostic as a plot, and reject it as a definition of a
dynamical family.

A later review confirms that unequal-mass continuation, topology, and
high-precision linear stability remain active \cite{li2025review}.
Equal-mass stable-orbit searches \cite{hristov2025stable} and restricted-problem
continuation papers are methodological neighbors, not this catalog.

\section{Evidence ladder}

A record may move only forward:
candidate $\to$ screened $\to$ closure verified $\to$
variational verified $\to$ independently reproduced $\to$ release claim.
Machine-learning output stops at \texttt{candidate}.
Float64 output stops at \texttt{screened}.
A manuscript sentence may use \emph{new}, \emph{discovered}, \emph{first},
\emph{stable} (as an ATLAS result), or a bifurcation name only from a
\texttt{release\_claim}.
The four gates are:

\begin{description}
\item[A] Independent BigFloat plus physical $E^\omega/E$ bind for every
mechanism word the paper uses.
\item[B] Connected critical graph: every published S/U cell assigned to exactly
one mechanism polyline (the number of polylines need not be $620$), every edge
end classified, no ``Newton failed'', completeness frozen.
\item[C] Family identity by continuation, not by $(T,L)$ clusters.
\item[D] Assembler \texttt{release\_ready}, same-day novelty,
\texttt{--require-solved}.
\end{description}
Gate~C has passed.
Gate~A has passed for the mechanism words used below.
Gates~B and~D are open, so the project is OPEN.

\section{Equations and the universal Floquet domain}

Planar Newtonian motion with $G=1$ is
\begin{equation}
\ddot{\mathbf r}_i=\sum_{j\ne i}m_j\frac{\mathbf r_j-\mathbf r_i}{\lVert\mathbf r_j-\mathbf r_i\rVert^3}.
\end{equation}
Screening uses DOP853 in IEEE binary64.
Publication truth uses an independent Julia BigFloat Vern9 stack and the
physical symplectic quotient $E^\omega/E$ in canonical Jacobi coordinates,
$E=\mathrm{span}\{X_H,X_L\}$, not deletion of four raw multipliers.

After center-of-mass reduction the monodromy has four trivial/symmetry unit
multipliers.
The remaining four form two reciprocal pairs.
With $t=\lambda+\lambda^{-1}$,
\[
P(t)=t^2-(\alpha-4)t+\beta-4\alpha+8,
\]
where $\alpha=\mathrm{tr}\,M$ and
$\beta=((\mathrm{tr}\,M)^2-\mathrm{tr}(M^2))/2$.
Spectral stability of both pairs is equivalent to both roots of $P$ lying in
$[-2,2]$.
The three walls are
\begin{align}
P(+2)&=\beta-6\alpha+20=0,\\
P(-2)&=\beta-2\alpha+4=0,\\
\Delta&=\alpha^2+8\alpha-16-4\beta=0.
\end{align}
The exact spectral vertices are
$(0,-4)$ (double $-1$), $(4,4)$ (mixed $+1/-1$), and $(8,28)$ (double $+1$).
A smooth $+1/-1$ switch on one continuation sheet must pass through $(4,4)$
unless a coarse cell aliases several roots.
The mass-space critical set is the preimage of this universal boundary under
the continuation-sheet map $(m_1,m_2)\mapsto(\alpha,\beta)$.

\section{Reproduction of the published table}
\IfFileExists{generated/reproduction.tex}{\input{generated/reproduction.tex}}{No frozen reproduction results have yet been inserted.}

\section{Authorized ATLAS results}
\IfFileExists{generated/discoveries.tex}{\input{generated/discoveries.tex}}{No new scientific claims have yet passed the release gate.}

\begin{table}[ht]
\centering
\caption{Independently bound critical objects.
Masses have $m_3=1$.
Uncertainties are independent-bracket widths, not formal theorems.}
\label{tab:bound}
{\small
\begin{tabular}{@{}llll@{}}
\toprule
Object & $m_1$ & $m_2$ & Mechanism \\
\midrule
Lower $+1$ & $0.8023446114$ & $0.7540187220$ & physical $+1$, width $1.58\times10^{-9}$ \\
Upper HH & $0.8022889781$ & $0.7592347066$ & opposite-Krein, width $6.27\times10^{-9}$ \\
Mixed L & $0.9292391921$ & $0.8853664625$ & $(\alpha,\beta)=(4,4)$ \\
Mixed S & $0.9967681995$ & $0.9560193602$ & $(\alpha,\beta)=(4,4)$ \\
Mixed R & $1.0495531867$ & $1.1294758073$ & $(\alpha,\beta)=(4,4)$ \\
\bottomrule
\end{tabular}}
\end{table}

\section{Screening picture, not a release graph}

On the published $0.001$ grid every one of the $620$ adjacent S/U cells has
exactly one endpoint-sign-changing smooth event:
$198$ of type $+1$, $168$ of type $-1$, $254$ trace collisions.
Coarse tracks:
principal lower $+1\to-1\to+1$,
principal upper $\Delta=0$,
secondary lower $-1\to+1$,
secondary upper $-1$.
That census is a seed, not the graph.
Its worst localized residual is $\lvert\text{event}\rvert=1.9898\times10^{-8}$
against the frozen gate $2\times10^{-8}$, a margin of about $0.5\%$, with $165$
of the $620$ roots above $10^{-8}$; the gate is cleared, but not comfortably.

\paragraph{The event gate is tight for float64, not for the organizers.}
Re-evaluating the three canonical $60$-digit organizer charts with this
repository's float64 stack gives $+1$ events of
$-1.2794\times10^{-7}$ (principal-left),
$-1.9582\times10^{-8}$ (secondary-left) and
$-2.5555\times10^{-8}$ (principal-right), so two of the three canonical
organizers do not themselves clear the frozen $2\times10^{-8}$ event gate when
they are merely evaluated in float64.  At $60$ digits the same charts give
relative $+1$ events of order $10^{-14}$.  This is a statement about the gate
under float64 evaluation, not about the organizers: a float64 reproduction must
re-correct, not re-evaluate, and the gate must not be loosened to accommodate
the difference.
An off-grid active-learning screen of $12$ proposals all corrected onto the
known sheet and none were screening-stable: no hidden island in that sample.

\paragraph{How little ``completeness'' covers.}
The word \emph{complete} is used here only in a bounded sense, and the bound is
small.  Completeness has exactly two inputs, and neither is a sample of the
declared domain
$m_1\in[0.8,1.1]$, $m_2\in[0.7,1.2]$ (area $0.15$).
The finer one is the frozen stability-neck raster
(\texttt{research/evidence/V1\_NECK\_RASTER\_2026-08-16.json}) over
$m_1\in[0.997,0.999]$, $m_2\in[0.993,1.012]$ at step $10^{-4}$
($4011$ corrected samples, all $21$ lines \texttt{separated}, zero interior
merges and zero truncation-undecidable lines, minimum resolved unstable gap
$3.0\times10^{-4}$, maximum shooting residual $4.7807\times10^{-10}$).
That rectangle is
$2.533\times10^{-4}$ of the declared area: \textbf{0.0253\%}.
The other input is the $12$-proposal active-learning screen, and it is a
pocket, not a sample: all twelve proposals lie in
$m_1\in[0.80005,0.80276]$, $m_2\in[0.75266,0.75407]$ at the principal lower
transition, an area fraction of $2.539\times10^{-5}$
(\textbf{0.002539\%}), and that artifact declares itself ``AI proposals plus
float64 screening only; not scientific discovery evidence''.
Outside the rectangle and that pocket, ``no hidden stability pocket'' rests on
the published $0.001$ grid alone.
Both figures are derived, not asserted:
\texttt{scripts/completeness\_scope.py} reads the raster extent from the
\emph{merge} job of \texttt{.github/workflows/stability-neck-scan.yml}
(the tile job carries its own \texttt{--step}, which is not the frozen one),
cross-checks the rectangle, step and sample count against the committed raster
artifact, reads the pocket from the screen artifact, and takes the declared
domain from \texttt{scripts/assemble\_critical\_graph.py}; a test fails if a
number quoted here drifts from it.

\paragraph{The assembled graph is not yet connected.}
Continuation-connectedness of the family sheet (Gate~C) and connectedness of
the critical graph are different statements, and the second does not follow from
the first.  In the committed
\texttt{research/evidence/V1\_CRITICAL\_GRAPH.json}, two edge ends are still
unclassified, so the edge incidence splits into three components ---
$\{$domain-$m_1$-min-$m_2$-$0.756$, mixed-L, mixed-R,
domain-$m_2$-max-$m_1$-$1.071\}$,
$\{$mixed-S, secondary-right-death$\}$ and
$\{$domain-$m_1$-min-$m_2$-$0.761$, domain-$m_2$-max-$m_1$-$1.053\}$ ---
while three further declared nodes carry no edge at all, and
\texttt{secondary\_left\_birth} is the one unexplained node.
Attachment of an edge end to an organizer is also threshold-dependent, and the
threshold does double duty: \texttt{GERM\_ATTACH\_DISTANCE}$=0.008$ caps both
how far a germ may sit from its organizer and how far an edge end may sit from
that germ, so the effective organizer-to-endpoint reach is $0.016$.
Auditing the release configuration shows the present six-attachment assembly
keeps every attachment it has only on the half-open window
$[0.006837,0.009431)$, $1.379\times$ wide: the largest accepted attachment is
$0.006837449$ and the nearest rejected mode-matching candidate is $0.009430577$.
The assembled graph does not visibly change until $0.010349059$
($1.514\times$), because that rejected candidate's endpoint is already bound by
its own classification artifact.
One germ trace was tuned to reach an endpoint: the secondary-left germs used
\texttt{--step}~$0.003$ rather than the $7.5\times10^{-4}$ default so the
$-1$ germ would carry far enough to bring cell $392$'s endpoint
($8.59\times10^{-3}$ from the organizer) inside the radius.  It did not also
sweep up the blocked endpoint --- \texttt{minus\_one\_s\_to\_u\_0}'s start is
$2.81\times10^{-2}$ from the organizer, past even the composed $0.016$ reach ---
which is what keeps that disclosure honest rather than convenient.
\section{The event gate has almost no margin, and less resolution than margin}
\label{sec:margin}

The localized census is gated at $|G|\le 2\times10^{-8}$ and $\|\text{closure}\|
\le 10^{-7}$.  Both gates are frozen and are never loosened anywhere in this
work.  What follows is a statement about how much that first gate is worth.

Across the $620$ localized roots the event residual has median
$3.29\times10^{-9}$, ninetieth percentile $1.58\times10^{-8}$, ninety-ninth
percentile $1.90\times10^{-8}$, and maximum $1.9897980818583960\times10^{-8}$.
The census therefore passes its own gate by $0.511\%$: the worst cell occupies
$99.489\%$ of the budget.  $165$ of the $620$ roots exceed $10^{-8}$ and seven
exceed $95\%$ of the gate.  Any sentence reporting that the census ``passes the
$2\times10^{-8}$ gate'' is incomplete unless it also reports that margin.

The margin is not the binding problem.  The binding problem is that
$2\times10^{-8}$ is finer than the accuracy with which the event functional can
be evaluated at these orbits.  Holding the recorded state fixed and recomputing
$G$ with the locked dependency versions, moving only the integrator tolerance
from $\mathrm{rtol}=5\times10^{-13}$ to $5\times10^{-14}$, moves $G$ by a median
$1.48\times10^{-7}$, which is $7.4$ times the gate; $20$ of $24$ probed cells --
the twelve worst offenders and twelve evenly spaced controls alike -- cross
$2\times10^{-8}$ at some tolerance on that ladder.  Re-running the identical
computation on a different CPU architecture, same locked
\texttt{numpy}/\texttt{scipy}, reproduces every digit locally but differs from
the frozen values by a median $2.78\times10^{-8}$ and a maximum
$4.14\times10^{-7}$; ten of twenty-two comparable probes land above the gate.
The mechanism is transparent: $\Delta=(\alpha-4)^2-4(\beta-4\alpha+8)$ with
$\alpha,\beta=O(1)$ cancels roughly eight decimal digits, and the surviving
digits sit on architecture-dependent summation order inside the monodromy
product.

Two consequences, stated separately because they are not the same claim.
First, for the roughly $165$ cells in the top decade the accepted float64
residual lies below that lane's own evaluation noise, so $|G|\le
2\times10^{-8}$ there records where the localizer stopped, not how close the
point is to the true critical set.  The same stopping rule is visible in the
independent BigFloat lane, which also terminates on the first iterate under the
gate and consequently reports seven of its $158$ roots at $10^{-8}$ to
$1.66\times10^{-8}$ despite $40$-digit arithmetic.
Second, the \emph{locations} are much better than the certificate.  The measured
$\partial G/\partial m_2$ ranges from $20$ to $660$, so even the full evaluation
uncertainty maps to an $m_2$ uncertainty between $10^{-10}$ and $8\times10^{-9}$,
far inside the $10^{-3}$ published cells.  The graph's combinatorics are not at
risk; the numerical certificate attached to them is weaker than its own
tolerance suggests.

The tail does not cluster.  Among the twenty-five worst cells the event-mode
enrichment is $1.27$ for $\Delta$, $1.00$ for $+1$ and $0.59$ for $-1$;
orientation enrichment is $1.07$ versus $0.93$; they occupy twelve distinct
$m_1$ bands; the Spearman correlation between $|G|$ and $m_1$ is $0.12$; and
their median distance to the nearest organizer, $0.054$, is indistinguishable
from the census median $0.049$.  A physical cause would concentrate.  This is
scattered, which is what a measurement floor looks like.

That floor is quantifiable.  Across the tight tolerance ladder the monodromy
invariants $\alpha,\beta$ themselves are reproducible only to a median relative
$4.2\times10^{-9}$ -- about nine significant digits, not fifteen -- and the
event functionals cancel by a median factor $2.6\times10^{8}$ (worst
$5.2\times10^{10}$).  The product of those two numbers is the observed
$10^{-7}$.  Pulling $\max|G|$ down by a factor of ten to $2\times10^{-9}$ is
therefore not reachable in float64 at any integrator tolerance: the target sits
$73$ times below that lane's own evaluation uncertainty.  It is reachable in the
BigFloat lane, which already attains $7\times10^{-13}$ on its best cells and
whose $10^{-8}$ entries are gate-limited rather than precision-limited; the cost
is running all $620$ cells there instead of $158$.  At the measured mean of
$5.45$ secant iterations per cell and the measured dps$=60$ correction cost of
$5.5$ minutes, that is about $23$ minutes per cell, $179$ serial CPU-hours, or
under three wall-hours at the existing $62$-way CI fan-out.

Accordingly every solve in this work now reports, alongside its residual, the
singular values $\sigma_{\min}$, $\sigma_{\max}$, the condition number
$\kappa_2=\sigma_{\max}/\sigma_{\min}$ of its Jacobian, and the backward-error
displacement bound $\|\delta x\|\lesssim\|J^{+}\|\,\|F(x)\|$.  A residual
without its conditioning is not a measurement.

\section{What remains, and why the status is OPEN}

\begin{enumerate}
\item Classify the two unclassified edge ends so the graph's edge incidence is
one connected object rather than three components plus four unattached nodes.
\item Classify the secondary-left $G_-$ birth (fold candidate near
$(0.9957,0.9743)$) and the secondary-right death between $m_1=1.042$ and
$1.043$.
\item Attach event arcs through the three mixed roots.
\item Classify the physical-soft $+1$ daughter, or falsify it.
\item Flip \texttt{release\_ready} only with
\texttt{scripts/assemble\_critical\_graph.py}.
\item Rerun the novelty search on the freeze day and run
\texttt{--require-solved}.
\end{enumerate}
Until those close, a sentence of the form ``we computed the connected
stability-boundary manifold'' is false.

\section{Discovery release status}
\IfFileExists{generated/discovery-release.tex}{\input{generated/discovery-release.tex}}{No machine-readable discovery status was injected into this manuscript build.}

\section{Reproducibility}

The Python screening stack is CPython~3.13, locked with \texttt{uv}.
The independent verifier is Julia~$1.11.9$ + BigFloat + Vern9 + GenericSchur.
JAX is optional discovery infrastructure and is not a publication dependency.
Every released number in Table~\ref{tab:bound} is backed by a frozen JSON
record under \texttt{research/evidence/}.
GitHub Actions run identifiers are stored in
\texttt{research/DISCOVERY\_RELEASE.json}.

\bibliographystyle{plain}
\bibliography{references}
\end{document}
